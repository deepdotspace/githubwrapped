/**
 * Navigation — intentionally empty.
 *
 * GitHub Wrapped is a full-bleed, immersive scroll experience: the landing and
 * the story each own the entire viewport, so there is no app-level nav bar. This
 * component is kept as a no-op so any lingering import stays valid; the shell in
 * _app.tsx no longer renders it.
 */
export default function Navigation() {
  return null
}
